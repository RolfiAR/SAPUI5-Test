_schema-version: "3.2"
ID: FirstProject
version: 0.0.1
modules:
- name: firstproject-approuter
  type: approuter.nodejs
  path: firstproject-approuter
  requires:
  - name: FirstProject_html_repo_runtime
  - name: FirstProject-dest-srv
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: FirstProject_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: FirstProject_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - TestPackageui5module.zip
      name: TestPackageui5module
      target-path: resources/
- name: TestPackageui5module
  type: html5
  path: ui5module
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: FirstProject_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: FirstProject_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: FirstProject-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: FirstProject-dest-srv
    service-plan: lite
parameters:
  deploy_mode: html5-repo
